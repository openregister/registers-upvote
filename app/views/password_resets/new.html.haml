.grid-row
  .column-full
    %h1.heading-large Forgot your password?

    %p We’ll send you an email to create a new password.

    = form_tag password_resets_path, method: :post do
      .form-group
        = label_tag :email, 'Email address', class: 'form-label'
        = text_field_tag :email, params[:email], class: 'form-control'

      .form-group
        = submit_tag 'Continue', class: 'button button-primary'
